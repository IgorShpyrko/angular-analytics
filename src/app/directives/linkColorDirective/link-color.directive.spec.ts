import { LinkColorDirective } from './link-color.directive';

describe('BtnColorDirective', () => {
  it('should create an instance', () => {
    // const directive = new BtnColorDirective();
    // expect(directive).toBeTruthy();
  });
});
