import { BtnDirectiveDirective } from './btn-directive.directive';

describe('BtnDirectiveDirective', () => {
  it('should create an instance', () => {
    // const directive = new BtnDirectiveDirective();
    // expect(directive).toBeTruthy();
  });
});
