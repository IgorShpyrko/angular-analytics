import { PassHelpDirective } from './pass-help.directive';

describe('PassHelpDirective', () => {
  it('should create an instance', () => {
    const directive = new PassHelpDirective();
    expect(directive).toBeTruthy();
  });
});
