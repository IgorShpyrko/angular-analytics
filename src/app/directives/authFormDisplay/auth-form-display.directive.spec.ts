import { AuthFormDisplayDirective } from './auth-form-display.directive';

describe('AuthFormDisplayDirective', () => {
  it('should create an instance', () => {
    // const directive = new AuthFormDisplayDirective();
    // expect(directive).toBeTruthy();
  });
});
